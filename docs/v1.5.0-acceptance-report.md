# v1.5.0 Acceptance Report

**Owner**: King + ops-agent
**Released from branch**: `feature/v1.0-big-bang`
**Acceptance date**: 2026-04-27 (Asia/Shanghai)
**HEAD at acceptance**: `777da36`
**alembic head**: `a1001900`
**pytest baseline**: 222 passed in 4.13s, 0 failed, 0 flake

This report wraps up the v1.4.x → v1.5.0 plan documented in `docs/v1.4.x-v1.5.0-roadmap.md`. All Phase A (acceptance carry-over), Phase B (user-facing fixes), Phase C (v1.5.0 main line), and Phase D (test-suite hygiene) items are ✅ done. Verification gates green; truth-table snapshot recorded below.

---

## 1. Truth Table (acceptance snapshot)

### Postgres `aiwrite`

| Counter | Value | Notes |
|---|---|---|
| `reference_books` | 3 | 龙族全套 / 天之炽 / 天之炽②女武神 — Phase A baseline holds |
| `reference_book_slices` | 4115 | 1:1 with style/beat cards |
| `style_profile_cards` | 4115 | PG = Qdrant (`style_profiles`) |
| `beat_sheet_cards` | 4115 | PG = Qdrant (`beat_sheets`) |
| `outlines` | 18 | 9 book + 9 volume; partial UNIQUE `(project_id) WHERE level='book'` enforced |
| `projects` | 38 | up from 30 baseline (normal user creates) |
| `volumes` | 60 | unchanged from Phase A |
| `chapters` | 184 | unchanged structure |
| `chapters` (status=completed) | 3 | A5 chapter1 (5210字) + 2 follow-ups; remainder are draft/empty by design |
| `world_rules` | 58 | + new `metadata_json` JSON column (alembic `a1001900`) |
| `characters` | 11 | EntityTimelineService unified writer; B2′ |
| `cascade_tasks` | 0 | clean — smoke harness wipes per-round; UNIQUE idempotent |
| `chapter_evaluations` | 27 | populated since B1′ Evaluator P0 fix |
| `llm_call_logs` | 28136 | tier_used / fallback_reason / attempt_index columns live (B1, alembic `a1001501`) |
| `prompt_assets` (is_active=1) | 27 | including `scene_planner` (standard/4096) + `scene_writer` (flagship/8192) |
| `generation_tasks` | 13 | async outline + chapter run history |

### Qdrant collections

```
styles
chapter_summaries
beat_sheets
plots
style_profiles
style_samples_redacted
```

PG↔Qdrant invariants from Phase A2/A3 still hold; `style_samples_redacted` orphan over-write deferred to v1.6 cleanup.

### Test suite (commit `6cf95a9`+)

```
pytest tests/ -q --tb=no
222 passed, 8 warnings in 4.13s
```

3 consecutive runs all 222/222 PASS — 0 flake remaining.

---

## 2. Phase C (v1.5.0 main line) — done

| ID | Status | Reference commit(s) | Evidence |
|---|---|---|---|
| **C1** scene_planner + scene_writer staged writing | ✅ | `b316d42` (impl), `301b835` (22 unit tests), `e16b182` (alembic `a1001601` endpoint backfill) | `app/services/scene_orchestrator.py` ~302 lines; alembic `a1001600_v150_scene_writing` seeds 2 prompts (sys_len 647 + 443); `api/generate.py:37 use_scene_mode` flag + `:181-189` dispatch; PG rows `is_active=1 has_endpoint=t` |
| **C2** chapter_evaluator → auto-revise loop | ✅ | `faa28b3` (`evaluate_tasks` table, alembic `a1001700`), `4f6e9e0` (revise helpers), `2e31703` (celery task), `ed75630` (async API), `0f9ddd6` (revise loop wiring), `14d892e` (24 tests), `e70222f` (deadlock-break `db.rollback()` + `asyncio.timeout(900)` + `user_instruction` passthrough) | revise loop @ `api/generate.py:267-373`; condition `req.use_scene_mode AND overall < req.revise_threshold AND rounds < N(=2)`; AGENTS.md「Layer 1 — C2 fix」固化 |
| **C3** prompt cache (process-local TTL=300s + buffered flush) | ✅ | `289a121` (impl), `4ebc6b1` (AGENTS.md repo doc) | `app/services/prompt_cache.py:130 get_snapshot` + `:209 buffer_track_result` + `:237 flush_pending_counts` (background 30s independent session); `prompt_registry` read path migrated; AGENTS.md「Layer 2 — C3 fix」mandatory contract: hot path **must** use cache, **never** `PromptRegistry.{get,resolve_route,track_result}` |
| **C4** cascade auto-regenerate (cascade_planner + cascade_tasks + worker + 4 handlers) | ✅ | `bc198ce` (table), `cc45e89` (planner), `fced11d` (worker + enqueue), `e2e5f41` (auto-trigger on rounds_exhausted), `d1a9770` (planner unit tests), `7e7beec` (e2e double-round smoke), `beadbe2` (C4-7 outline handler), `d144212` (C4-8 character + world_rule handlers + alembic `a1001900`) | `_record_inline_cascade_revision` shared helper; rev_key `{source_chapter_id}:{severity}` idempotent; UNIQUE `(source_chapter_id,target_entity_type,target_entity_id,severity)` 防 redelivery; handlers 严守不动 planner ordering 字段 (`is_confirmed`/`name`/`rule_text`/`category`) |

C1+C2+C3+C4 commit chain (oldest → newest):
```
b316d42 → 301b835 → e16b182 → faa28b3 → 4f6e9e0 → 2e31703 → ed75630 → 0f9ddd6 →
14d892e → e70222f → 289a121 → 4ebc6b1 → bc198ce → cc45e89 → fced11d → e2e5f41 →
d1a9770 → 7e7beec → beadbe2 → 0fd42a1 → d144212 → b950078
```

---

## 3. Phase A / B / D — carried

- **Phase A (A1–A6)**: ✅ commit `1d602d6` aggregate. Evidence in `docs/v1.4.x-acceptance-report.md` (236 lines, 6/6 ✅).
- **Phase B**: B1 (`3d21aa9` tier-aware fallback + `llm_call_logs` columns), B2 (`a325157`/`0661d96`/`159c66a` Character/HAS_STATE writer 统一 + 49 GqlStatus warning 归零), B1′ (`6c5224b` evaluator/checker tier fallback), B2′ (Character writer 重构 + `entities.extract_chapter` celery + `ExtractionMarker` Neo4j 幂等).
- **Phase D (test hygiene)**: commit `6cf95a9` —
  - **D-1** 4× `tests/test_api_core.py` `RuntimeError: Future attached to a different loop` 根因 = pytest-asyncio v1.x 默认 per-function loop scope 与模块级 SQLAlchemy AsyncEngine 池冲突。`pyproject.toml` 加 `[tool.pytest.ini_options]` 三件套：`asyncio_mode="strict"` + `asyncio_default_test_loop_scope="session"` + `asyncio_default_fixture_loop_scope="session"`。
  - **D-2** `tests/test_v10_observability.py::test_metrics_endpoint_public` 断言 `http_request_total` (单数) → 实际 `http_requests_total` (复数)，一字之差。
  - 验证：3 轮 pytest 全 222/222 PASS，0 flake。

---

## 4. Validation status

### Code-level (this session)

- `from app.services.scene_orchestrator import SceneOrchestrator; SceneOrchestrator()` import + 实例化 干净。
- `prompt_cache.get_snapshot / buffer_track_result / flush_pending_counts` 三函数齐备。
- Celery worker `64493be5c567` ping OK；`entities.extract_chapter` + `cascade.run_cascade_task` + `evaluations.evaluate_chapter` 三 task 注册 OK。
- Alembic head = `a1001900`（即所有 v1.5.0 迁移已 apply：`a1001500` outline UNIQUE → `a1001501` llm_call_logs columns → `a1001600` scene_writing seed → `a1001601` scene endpoint backfill → `a1001700` evaluate_tasks → `a1001800` cascade_tasks → `a1001900` world_rules.metadata_json）。

### E2E baseline (carried from earlier sessions)

- **A5 baseline**: chapter1 (`932922e8-...`) SSE 5210 字 status=completed 落库 + 0 危险信号，证明 chapter SSE auto-save (Bug K) 修复有效。
- **B1′ baseline**: chapter1 evaluator HTTP 200 / 70.05s / overall=7.98（plot 8.4 / char 7.9 / style 8.2 / pace 7.8 / fore 7.6），16 条具体 issue，`llm_call_logs.tier_used=standard` + `attempt_index=0` + `fallback_reason=NULL`。
- **C4 e2e double-round**: `scripts/c4_e2e_smoke.py --rounds 2` round1 dispatched=1 worker terminal=`done`；round2 duplicates=1 dispatched=0（UNIQUE 跨轮幂等 ✅）。

### Pending optional validation

- ⬜ **真实 LLM scene_mode + auto-revise e2e**：用 chapter 4-15 任意一个未写章节触发 `use_scene_mode=true` + `revise_threshold=8.0` + `rounds=2`，观察 scene_planner JSON / scene_writer 流式 / 重写决策 / `chapter_evaluations` 多轮记录。预计单章 ~5-10 分钟（baseline + 1 轮 revise）。**未在本验收报告内执行**——code path + unit/integration 已覆盖；King 决定是否需要真 LLM 烟测。

---

## 5. Carry-forward / Known limitations

1. **Bug H** worker event-loop reuse warning（`reference_ingestor.py:473 retry_reference_book_missing_branches`）：Phase A6 deferred；A5 之后 0 新增；只在 reprocess 工作流触发。**v1.6 候选**。
2. **Qdrant `style_samples_redacted`**：每本 ~3× slice count，redact 分支无 PG dedup → retry 重复推点。Non-blocking for RAG recall；**v1.6 cleanup 候选**。
3. **C3 上游 prompt cache**：`AnthropicProvider.generate` / `OpenAIProvider.generate` 加 `cache_control: ephemeral` / `prompt_cache_key` 留作 v1.6 独立优化（C3 当前是进程级 prompt_assets snapshot 缓存，与上游 LLM 提供方的 prompt cache 是两个独立层）。
4. **scene_mode 观测面**：scene_planner JSON parse 失败回退到 `_fallback_scene_briefs` 没有专门 metric / 告警，仅 logger.warning。**v1.6 observability 候选**。
5. **Cascade 前端面板**：cascade_tasks 状态、handler 写入的 `cascade_revisions / cascade_hints` 在前端无可视化。**v1.6 UI 候选**。
6. **Tests hygiene**：`tests/conftest.py` 里 legacy session-scoped `event_loop` fixture 被 pytest-asyncio v1.x 忽略但仍残留——非阻塞，可清理。

---

## 6. Acceptance verdict

**v1.5.0 main line ✅ 整体达成，可发版。**

- 代码：4 个 v1.5.0 主线特性 (C1+C2+C3+C4) 全部上线，22 个 commit 覆盖 impl + tests + docs。
- DB：alembic head a1001900；7 个 v1.5.0 迁移全 apply。
- 测试：222 passed / 0 failed / 0 flake，3 轮稳定。
- 真值表：reference / outlines / chapters 与 Phase A 基线 1:1 对账无漂移。
- 已固化经验：AGENTS.md「prompt_registry 死锁双层防线」+ cascade handler 「严禁动 planner ordering 字段」+ pytest-asyncio strict mode session-loop scope。

剩余一项可选烟测（真实 LLM scene_mode + auto-revise）由 King 决定是否执行。其余 6 项 known limitation 全部进入 v1.6 backlog。

---

## Appendix A — Acceptance commit chain (full)

```
777da36 docs(roadmap): mark C1/C2/C3 done (commits b316d42 / e70222f / 289a121)   ← HEAD
8972132 docs(roadmap): mark D done (5 flakes eliminated, commit 6cf95a9)
6cf95a9 fix(tests): eliminate 5 pre-existing flakes (D-1 + D-2)
b950078 docs(roadmap): mark C4-8 character + world_rule handlers done (d144212)
d144212 feat(c4-8): implement character + world_rule cascade handlers (in-place revision)
0fd42a1 docs(roadmap): mark C4 cascade chain done (C4-7 outline handler, beadbe2)
beadbe2 feat(c4-7): implement outline cascade handler (in-place revision)
7e7beec test(c4): repo-tracked end-to-end cascade smoke + idempotency double-round
d1a9770 test(c4): cascade planner & task surface unit tests
e2e5f41 feat(c4): trigger cascade auto-regenerate on auto-revise rounds_exhausted
fced11d feat(c4): cascade run_cascade_task celery + enqueue helper
cc45e89 feat(c4): cascade_planner — issues_json -> upstream entity tasks
bc198ce feat(c4): cascade_tasks table + ORM + alembic migration
4ebc6b1 docs(agents): repo-scoped AGENTS.md with C2/C3 deadlock layer-2 defense
289a121 feat(c3): process-local prompt asset cache + buffered counter flush
e70222f fix(c2): forward user_instruction + revise timeout + db.rollback to break deadlock
14d892e test(c2): auto_revise helpers + EvaluateTask + dispatch (24 tests)
0f9ddd6 feat(api): scene-mode chapter auto-revise loop on low eval score
ed75630 feat(api): /api/evaluate/start + /tasks/{id} async eval endpoints
2e31703 feat(celery): evaluations.evaluate_chapter async task
4f6e9e0 feat(eval): auto_revise threshold + issues to revise instruction helpers
faa28b3 feat(db): evaluate_tasks table + ORM for async eval API (a1001700)
e16b182 fix(prompt): backfill scene_planner / scene_writer endpoint_id (a1001601)
301b835 test(c1): SceneOrchestrator regression suite (22 tests)
b316d42 feat(api): scene-staged chapter writing via SceneOrchestrator   ← C1 起点
```
