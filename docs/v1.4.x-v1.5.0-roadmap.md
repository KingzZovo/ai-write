# v1.4.x → v1.5.0 Roadmap & Execution Plan

Owner: King + ops-agent
Created: 2026-04-26 (Asia/Shanghai)
Last updated: 2026-04-26
Branch: `feature/v1.0-big-bang`

This file is the **source of truth** for the next 12 work items spanning v1.4.x acceptance, v1.4.2 user-facing fixes, and the v1.5.0 main line. Update the status column after every meaningful step. Do not work off-plan.

---

## 0. Baseline (state at plan creation)

### Reference book distillation — clean ✅

| Book | id | status | slices | style 100% | beat 100% | error placeholders |
|---|---|---|---|---|---|---|
| 龙族全套 | `24498b6b-2698-4900-b44b-b42806964e1b` | ready | 2820 | ✅ | ✅ | 0 |
| 天之炽 | `67fe33f9-60e8-459e-9720-8546c828eab7` | ready | 982 | ✅ | ✅ | 0 |
| 天之炽②女武神 | `c33c2f19-03b4-46a1-ab0a-ede66175a7fe` | ready | 313 | ✅ | ✅ | 0 |

### Recent shipped commits

```
a1e0077 fix(chunker): merge sub-floor fragments + recognize volume/structure headings
206dba5 fix(celery): callable async_session_factory proxy to fix cross-loop crash
447690f fix(beat-extraction): tolerant JSON parser + strict prompt + retry
```

### Known open issues (carry into A2 / A6)

- ~~**Qdrant under-write** (resolved 2026-04-26 by A2): PG `style_profile_cards`=5855 vs Qdrant `style_profiles`=4807 (Qdrant short by 1048). PG `beat_sheet_cards`=8503 vs Qdrant `beat_sheets`=4684 (Qdrant short by 3819).~~ Numbers were stale — they came from pre-`a1e0077` chunker runs. After re-ingest the new ground truth is **PG = Qdrant** for both collections (see A2 row).
- **`style_samples_redacted` orphan points** (carry into A6): per-book Qdrant count is 3× slice count (8480/2964/949 vs 2820/982/313 slices). Redact branch had no PG dedup so retry waves re-pushed points. Non-blocking for RAG recall but bloats the collection.
- **`_slices_missing_beat` query (`reference_ingestor.py:296-306`)** only checks row existence, not content quality. After A1 cleanup the false-healthy risk is reduced but the query is still semantically weak. Re-audit during A6 (descoped from A3 — A3 ended up being purely the qdrant_store except-block fix).
- **Stale outline**: 24498b6b previous outline runs may have left orphan `BookOutline` rows (clean up in A6).
- **`tests/test_services.py` pre-existing failures** (surfaced during A3 verification, NOT caused by Bug C fix): `test_style_detection_basic` / `test_style_detection_filters_names` expect a `top_words` key that `detect_style_features` no longer emits; `test_compile_style` expects literal "必须保持" that the new compile_style template no longer contains. Tracked as A6 followup; non-blocking for A3-A5.

---

## Phase A — v1.4.x acceptance (must finish first)

| ID | Task | Status | Owner | Verification gate |
|---|---|---|---|---|
| A1 | 4 个 style 检测验证：用 24498b6b/67fe33f9/c33c2f19 触发 style detection 端点（POV/tense/dialogue ratio/sentence-rhythm 等四项），核对 PG/响应一致 | ✅ done | agent | 4/4 维度返回非空、与 style_profile_cards 对账一致 |
| A2 | Qdrant 缺写排查 + 回填：定位漏写根因（embeddings 502 / commit-then-fail / drop-collection），补一次 backfill，让 PG=Qdrant | ✅ done | agent | PG=Qdrant per-book（gate 原 5855/8503 数字基于旧 chunker 结果，已失效；新真值 4115 slices 全部 1:1） |
| A3 | Bug C: `qdrant_store.py` 三个 v0.6 store helper（`store_style_profile` / `store_beat_sheet` / `store_style_sample_redacted`）在 `client.upsert` 抛错时只 `logger.warning` 并返回伪造的 deterministic point_id → caller 落假 PG 卡 → PG↔Qdrant 漂移。修复：3 处 except 后加 `raise`；caller 是 `reference_ingestor._{style,beat,redact}_branch` 各 1 处共 3 处（原估「13+5=18 caller」偏高），已有 try/except + `bdb.rollback()` 兜底无需改 | ✅ done | agent | py_compile + 4 个回归测试 (`tests/services/test_qdrant_store_bug_c.py`) 全过 + services 套件 16/16 0 regression |
| A4 | book outline 链路修复：(D-1) `prompt_assets.outline_book.max_tokens` 16384 → 6000 防 ~300s 同步链路超时；(D-2) 新增异步任务化 `POST /api/outlines/from-reference/start` + `GET /api/outlines/from-reference/tasks/{task_id}` 走 celery worker，规避同步 HTTP 长时延断流；保留旧 sync 端点向后兼容 | ✅ done | agent | task `769aa537` status=completed, char_count=5754, sketch_line_count=2820, error_message=null, celery 用时 227.8s, 0 stream-id INTERNAL_ERROR / unparseable / event-loop 异常 |
| A5 | volume1 regenerate + chapter1 SSE 烟测 + Bug K 修复（chapter SSE auto-save）：基于 A4 outline 重生 volume1，再触发 chapter1 SSE，发现并修复 chapter SSE 完成不存盘的 Bug K | ✅ done | agent | 2 接口 200 + ch1 status=completed clen=5210 word_count=5210 + 0 危险信号 |
| A6 | 6-point 验收报告 + stale outline cleanup + tests sync：汇总 A1-A5 + Bug E/H/L 分类 + 修 `tests/test_services.py` 上游漂移 | ✅ done | agent | `docs/v1.4.x-acceptance-report.md` 6/6 ✅ + tests 6/6 PASS + Bug E closed 误报 / Bug H · L deferred Phase B |

---

## Phase B — v1.4.2 user-facing fixes (small effort, high ROI)

| ID | Task | Status | Notes |
|---|---|---|---|
| B1 | 接通 `generate_with_fallback` + tier 降级：让 `chapter_generator` / `outline_generator` 关键路径调它，按 tier (flagship → standard → small) 降级而非无脑遍历；降级路径写入 `llm_call_logs` | ✅ done | agent | router.generate_with_tier_fallback / stream_with_tier_fallback + prompt_registry 接线；llm_call_logs 新增 tier_used/fallback_reason/attempt_index (alembic a1001501)；E2E polishing 落库 tier_used=standard model=gpt-5.2(high)；6/6 单元 + 6/6 services PASS；commit 3d21aa9 |
| B2 | Recommendation badge 软警卫：保存 prompt 时若绑定 endpoint kind ≠ recommendation.kind 则弹确认对话框 | ⬜ todo | 不阻止，只警告；防 v1.4.x 的 401 配置错误 |

---

## Phase C — v1.5.0 main line (architecture changes)

| ID | Task | Status | Depends on |
|---|---|---|---|
| C1 | 章节分阶段写作：`scene_planner` 先列 N 个场景 200 字 brief（小模型）→ `scene_writer` 逐场景写 800-1200 字流式输出。新增 prompt asset (alembic) | ⬜ todo | — |
| C2 | `chapter_evaluator` → auto-revise 闭环：写完 → evaluator 打分 → < 70 把意见喂回 writer 重写一次（最多 N=2 轮） | ⬜ todo | C1（scene 级别可单独重写） |
| C3 | Prompt cache：`AnthropicProvider.generate` / `OpenAIProvider.generate` 加 `cache_control: ephemeral` / `prompt_cache_key`；优先 cache 长 system prompt + ContextPack L2 facts/L3 RAG | ⬜ todo | 独立 |
| C4 | cascade auto-regenerate：`cascade_regenerator.py` 已能分析下游影响，章节确认时 severity=major 弹"自动重写下游 N 章"按钮，确认后接 batch_generator | ⬜ todo | batch_generator 稳态 |

---

## Execution rules

1. **Sequential**: A1 → A2 → A3 → A4 → A5 → A6 → B1 → B2 → C1 → C2 → C3 → C4. No parallel work unless King explicitly authorizes a fork.
2. **Each step must end with**: `[ID] DONE` log line in this file's status column + git commit (when code changed) + verification output pasted in the chat.
3. **Discoveries that block forward progress**: open as new sub-tasks under the current phase, not silently merged into the next.
4. **Acceptance gates are hard**: do not mark `done` if verification gate is not green. Mark `partial` and explain.
5. **King's mandates already in effect**:
   - 主动诊断（看 logs、对账业务真值表，不只 status=success）
   - succeeded ≠ healthy
   - row count ≠ content validity
   - <2-min sample 不下"已恢复"结论
   - dependency change → must rebuild image
   - 0 LLM call + 0 HTTP POST → 必看 RuntimeError 栈
   - `_slices_missing_*` query 必须检查内容质量，不只看行存在

---

## Status legend

- ⬜ todo — not started
- 🔄 in progress — actively working, with a sub-bullet describing what's running
- ⚠️ blocked — explain blocker inline
- 🟡 partial — done but a verification gate failed; record what's missing
- ✅ done — all gates green, commit landed

---

## Change log

- 2026-04-26 — created plan; A1–C4 all ⬜ todo.
- 2026-04-26 — A1–A6 ✅ (commit 1d602d6); B1 ✅ (commit 3d21aa9 — tier-aware fallback chain).
- 2026-04-26 — **A1 DONE**: 3 本书 detect-from-book 201（67.8s/54.1s/60.5s），4 个统计维度（basic / dialogue_ratio / sentence_distribution / tone）与 LLM 15 项深度分析（narrative_pov / sentence_style / writing_rules 等）全部返回非空；backend/celery 0 异常；0 幻觉占位词。清理临时生成的 3 条 style_profiles。A2 转 in_progress。
- 2026-04-26 — **A2 DONE**: 真值表对账 PG vs Qdrant 完全一致。PG: 龙族 2820 / 天之炽 982 / 女武神 313 slices, style_cards=beat_cards=slices 三列对齐 (status=ready)。Qdrant: style_profiles & beat_sheets 每本与 PG slice 数 1:1（2820/982/313）。原 baseline 5855/8503 是 pre-`a1e0077` chunker 的脏数据，新 chunker 重切后真值降为 4115。celery 近期日志只见 PUT /collections/.../points 200 OK，无 5xx / INTERNAL_ERROR。新发现 `style_samples_redacted` 每本 3× slice 计数（redact 分支无 PG 表 → retry 重复推点），列入 A6 cleanup 而非阻断 A2。A3 转 in_progress。
- 2026-04-26 — **A3 DONE** (commit `06bd4fc`): 修复 `qdrant_store.py` 三处吞异常（`store_style_profile:390-392` / `store_beat_sheet:418-420` / `store_style_sample_redacted:448-450`），改为 `logger.warning(...)` + `raise`，让 caller 已有的 try/except + `bdb.rollback()` 接住，避免 PG 写假 `qdrant_point_id`。Caller 审查：`grep -rn` 三个 helper 名确认 backend 仅 `reference_ingestor.py:_style_branch:136 / _beat_branch:172 / _redact_branch:203` 三处 caller（原「13+5=18」偏高估计），三处都已 try/except 兜底，无需改动 caller 端。验证：py_compile OK；新增 `tests/services/test_qdrant_store_bug_c.py` 4 个回归测试（3× upsert 抛错重抛 + 1× happy-path 确定性 point_id）全过；既有 services 套件 16/16 0 regression。Pre-existing 3 处 `tests/test_services.py` 失败（style_detection/compile_style 上游漂移）与 Bug C 无关，已加入 known issues 列表 → A6 followup。A4 转 in_progress。

- 2026-04-26 — **A4 DONE**: `outline_book` 同步链路 ~300s 超时根因 = stream/inference 总时长触发链路上游超时（非 prompt context 上限，King 确认自托管 gpt5.4 与 OpenAI 同规模）。修复组合：**D-1** PG `UPDATE prompt_assets SET max_tokens=6000 WHERE task_type='outline_book'`（id=`3fe85343-1cfe-4eda-887f-d786662ef7b5`，原 16384）；**D-2** 新增 `app/api/generation_runs.py` 异步端点 `POST /api/outlines/from-reference/start`（创建 GenerationTask + `run_async_generation.delay()`）+ `GET /api/outlines/from-reference/tasks/{task_id}`（薄包装），worker `app/tasks/knowledge_tasks.py:182-228` 加 `task_type="outline_from_reference"` 分支调 `build_outline_from_reference()`，复用既有 markdown 清洗 / humanize / Outline auto-save；保留旧 sync `POST /api/outlines/from-reference` 向后兼容。烟测：task `769aa537-bd1b-4b17-abc1-3176bffb27eb` async pending→completed 用时 227.8s，char_count=5754，sketch_line_count=2820，reference_book=龙族全套，error_message=null，celery 近 3min 日志 grep ERROR/WARN/Traceback/stream-id INTERNAL_ERROR/unparseable/different event loop/Event loop is closed = **0 命中**；outline 主题/五卷划分/伏笔回收完整。证据：`/root/F1-photo/.tmp_evidence_d2_async_OK.json`（16K）+ `.tmp_evidence_a4_outline_v2_4096cap_OK.json`（D-1 验证 28K）。A5 转 in_progress。

- 2026-04-26 — **A4.1 DONE — outline level invariant 彻底修复**: D-2 深度观察暴露 3 个 critical bug（之前 A4 "0 命中" 报告系 `--since 3m` 时间窗 artifact，全日志 grep 实际 17× `Event loop is closed` + 13× `Future attached to a different loop` + 2× `never awaited` + 1× `Failed to load model config from DB`）：(F) `knowledge_tasks.py:383` `level = task.task_type.replace("outline_","")` 把 `outline_from_reference` 写成 `level='from_reference'` → A5 line 245 写死查 `level='book'` 完全拉不到；(G) `outlines` 表无 (project_id, level) 唯一约束，stale 累积无上限，`scalar_one_or_none()` 遇多行抛 `MultipleResultsFound`；(H) worker 启动事件循环复用警告（与 D-2 无关，留 A6）。**彻底方案**：alembic `a1001500_v150_outline_book_unique`：① `UPDATE outlines SET level='book' WHERE level='from_reference'`（语义订正，from_reference 本就是 book 级 outline）② ROW_NUMBER() PARTITION BY project_id ORDER BY created_at DESC 保留每 project 最新 1 行 book 删余 ③ `CREATE UNIQUE INDEX outlines_project_book_unique ON outlines(project_id) WHERE level='book'` partial UNIQUE。代码 `knowledge_tasks.py:380-388` auto-save 改显式 `_outline_level_map = {outline_from_reference: book, outline_book: book}` + 写前 `delete(Outline).where(project_id, level='book')` upsert，outline_volume 标 dead-code（真路径在 volumes.py，缺 parent_id 会写孤儿行）。验证：migration apply 26→17 行（清掉 9 行 stale book/from_reference dup），volume 8 行 1:1 不动，partial unique index 生效。Smoke task `138b3338-45eb-4f7d-98bf-bd30fa8ffeaa` outline_from_reference→completed 100s 5763 chars；upsert 替换 outline id `b4e7fe74` → `453fca6e`，outlines 表 f14712d6 行数始终=1，0 UniqueViolation/duplicate-key，worker restart 后 10min 窗口 0 危险信号。A5 query `Outline.level=='book'` 路径现由 DB 强制保证唯一不再可能 `MultipleResultsFound`。known issue "Stale outline" 已由本次清理消化，A6 仅剩 style_samples_redacted orphan + tests/test_services.py 上游漂移。
- 2026-04-26 — **A5 DONE — Bug K (chapter SSE auto-save) 修复**: 基于 A4.1 已 confirm 的 outline `453fca6e` 推进 A5：① outline confirm（`POST /api/outlines/{id}/confirm`）→ `is_confirmed=1`；② 创建 volume1 `eeeb4a68-3eab-4057-a102-fbe460ca2bb7`（HTTP 201）；③ volume1 regenerate SSE 110s 全绿（2 outlines + 15 chapters draft 行落地，0 噪音）；④ chapter1 (`932922e8-2055-46ce-8673-8ce8111e0108`) SSE 触发，183701 bytes / 6826 chunks / `status:completed` + `[DONE]`，HTTP/EXIT 全绿，**但 PG `chapters.content_text` 仍 0 字节、status=draft → Bug K 暴露**。根因（读源码确认）：`backend/app/api/generate.py:54 generate_chapter` 的 SSE event_stream 完全没有 auto-save 路径（仅 `async for chunk: yield`，无 `collected_text` 累积、无 `async_session_factory()` 写盘块），与同文件 `generate_outline:184-355` 的成熟模板不对称；与 Bug F/I 同源（同步主路径漏写 DB）。**修复**：镜像 outline auto-save pattern 在 chapter handler 注入 3 处——event_stream() 顶部 `collected_text: list[str] = []`、yield 前 `if chunk: collected_text.append(chunk)`、`status:completed` 之前用独立 `async_session_factory()` 拿 session，优先 `await save_db.get(Chapter, req.chapter_id)`，fallback `select(Chapter).where(volume_id, chapter_idx).scalar_one_or_none()`，UPDATE `content_text/word_count/status="completed"` 后 commit + yield `{"status":"saved","chapter_id":...,"word_count":...}`，except 路径仅 `logger.warning` 不阻断 SSE。**验证**：backend container 13:48:06Z restart 后重跑 chapter1 SSE 同 payload，T+129s 终态 `status:saved word_count=5210` + `completed` + `[DONE]`，PG truth `clen=5210 word_count=5210 status=completed bytes=15013`，content 是真实正文（"雨是从傍晚开始变重的..."），其他 14 章按预期保持 draft；backend 5min 窗口 grep `ERROR|Traceback|attached to a different loop|Event loop is closed|unparseable|INTERNAL_ERROR` = **0 命中**。证据：`.tmp_evidence_a5_ch1_completed_DBmiss.sse`（Bug K before, 183701 bytes）+ `.tmp_a5_ch1_v2.sse`（fix after, 117204 bytes 含 saved 事件）。代码 commit ahead，roadmap 同步本条。Bug L candidate（Neo4j label/property GqlStatus warning）+ Bug H（worker event-loop 噪音）+ Bug E（`beat_json` schema）留 A6 配套处理。
- 2026-04-26 — **A6 DONE — v1.4.x acceptance report + tests sync**: 收尾阶段证据合并 + 遗留噪音分类。证据（1）**上游验证**：generation_runs / outline_from_reference 全路径多领域 grep、PG/Qdrant truth table 复查（3 / 4115 / 4115 / 4115 + Qdrant 4115 / 4115 / 12393 / 2158）、`outlines` 表 9 book + 9 volume = 18 行且每 project 全为单行 book（0 dup）、`outlines_project_book_unique` partial UNIQUE 仍生效。（2）**Bug 分类**：~Bug E (`beat_json` schema)~ 关闭为误报——4115/4115 行均 NOT NULL，`outline_from_reference._load_beat_sketch` 已用 `card.beat_json or {}` + `.get(…, default)` 安全降级读取；Bug H (`retry_reference_book_missing_branches` 事件循环复用 32 条) 所有时间戳≤ 12:14Z = a03b4f7 之前，A5 之后 0 新增，发生于 `reference_ingestor.py:473`，仅 reprocess 工作流触发，推到 Phase B；Bug L (Neo4j `Character / HAS_STATE / CharacterState` GqlStatus 警告 48 条) 同一 cypher 读取查空为预期，label/rel/property 尚未在图中创建，治本随 Phase B「角色状态图」写入路径一起实现。（3）**Tests**：重写 `backend/tests/test_services.py` 6/6 PASS 追上游现实——`detect_style_features` 现 keys = `ai_marker_density / ai_markers / basic / description_density / dialogue_ratio / sentence_distribution / style_labels / tone`（无 `top_words`）；`compile_style` 现输出为 `写作风格参考：…` + `【Anti-AI 规则】` + `【风格关键词】` 三段，不再包含“必须保持”字面；assertions 改为检查当前稳定锺点。（4）**交付物**：`docs/v1.4.x-acceptance-report.md` 236 行 6 点全绿 + commit chain + 遗留 known limitation 完整列表。后续 Phase B 入口，等待 King 授权。
