# Handoff — 2026-05-17 00:20 (Asia/Shanghai)

> 仅供下一个会话接手。复制本文全部内容到新窗口作为首条提示词，然后等用户给出新任务。

---

## 1. 你是谁

你是 Notion 里的定制 Agent，被接手接手一个“AI 写作后端工程维护”会话。用户名 King，备份项目根路径是 `/root/ai-write`，分支 `main`。说中文，少说多做，机制级修复 > 黑名单，不主动 offer。

## 2. 上一个会话刚做完的事（勿重复）

### v1.14 commit `6dda3c9` —— LLM transient-failure auto-retry + wave-batching

**新增** `backend/app/services/llm_retry.py`：

- `call_with_retry(fn, *, label, attempts=4, base_delay=2.0, max_delay=30.0)` — 零参 async callable + 指数退避 + full-jitter
- `_is_retryable(exc)` 识别：
  - **可重试**：httpx 传输错误（ConnectError/ReadTimeout/...）、openai SDK（APIConnection/Timeout/RateLimit/InternalServer/APIError）、通用 Timeout/Connection/OSError、字符串含 `HTTP 5xx` 或 `HTTP 429` 的 RuntimeError
  - **不重试**：其他 4xx（鉴权/参数错误）、JSON parse 错误

**修改** `backend/app/services/model_router.py`：三个出口都包了一层 `call_with_retry`

- `OpenAIProvider.generate` (L227) — 流式 chat completion，整个 stream 读取装进 inner fn、失败重启
- `EmbeddingProvider.embed` (L297) — OpenAI-compat embeddings
- `NvidiaEmbeddingProvider._post` (L383) — NVIDIA NIM；key 选择移到 inner，attempt #2 自动 round-robin 到备用 key

**修改** `backend/app/services/reference_ingestor.py`：

- 新增 `_RETRY_WAVE_BATCH = int(os.getenv("DECOMPILE_RETRY_WAVE_BATCH", "250"))`
- `retry_missing_branches` 主循环在 `asyncio.gather` 前对 `missing_style_rows` / `missing_beat_rows` 切 `[:250]`
- 原因：单波 20k+ slice 在 semaphore=3 下要跑 ~28h，超 celery visibility_timeout=7200，会静默重排 → 重复并发 worker

**修改** `backend/app/tasks/__init__.py:retry_reference_book_missing_branches`：进度感知调度

- 若 `style_filled + beat_filled > 0` → `countdown=DECOMPILE_RETRY_FAST_DELAY`（默认 30s）重排，并 reset `attempt=1`
- 若 0 progress 且 attempt < max_auto_retries → 按原 `compute_retry_delay`（指数退避）
- 若 0 progress 且 attempt 已满 → 不重排

## 3. 现在后台正在跑什么

- **Book**：《赤心巡天》 `0a543b1d-19fe-4e03-986e-42844feb36ee`（`status=partial`，22941 slices）
- **Celery task**：`retry_reference_book_missing_branches`，初家任务 id `8f1c8413-d13e-4599-bcc4-978c80ed3886`，于 Asia/Shanghai 14:08 被 worker 接走；任务自循环（有进度 = 30s 一波，reset attempt）
- **进度快照**（截至 16:08, +2h）——不要拿这个当现状，要重查：
  - style cards: 16696 → 16993 （+297，剩 5948）
  - beat cards: 9265 → 9324 （+59，剩 13617）
  - beat 增量少是**正常**现象（见 §4）

### 查看进度的 SQL

```bash
BID=0a543b1d-19fe-4e03-986e-42844feb36ee
docker exec ai-write-postgres-1 psql -U postgres -d aiwrite -tAc "\
SELECT (SELECT count(*) FROM style_profile_cards WHERE book_id='$BID') AS style, \
       (SELECT count(*) FROM beat_sheet_cards WHERE book_id='$BID') AS beat, \
       22941 AS total"
# 书表状态 / decompile_retry metadata
docker exec ai-write-postgres-1 psql -U postgres -d aiwrite -tAc "\
SELECT status, metadata_json::jsonb #>> '{decompile_retry}' \
FROM reference_books WHERE id='$BID'"
```

### 查看 worker 日志（docker logs --since 会超时，用 --tail）

```bash
docker logs --tail 4000 ai-write-celery-worker-1 2>&1 \
  | grep -E 'retry_reference_book_missing_branches\[|llm_retry|RateLimit|HTTP 429|HTTP 5|APIConnection|APITimeout|style_filled|beat_filled' \
  | tail -25
```

## 4. 已知「似是 bug 实际不是」的现象

### codex provider 最近 67h 冷却 + auth_not_found

用户 12:13 明确说：“没事，这个是正常的，重试就行了。” 不要再股动去动 router 配置。

具体表现：

```
gpt-5.5(xhigh)  RateLimitError 429: model_cooldown
                reset_seconds ~ 241000+ (≈67h)
                "All credentials for model gpt-5.5(xhigh) are cooling
                down via provider codex"
gpt-5.4(xhigh)  InternalServerError 503: auth_not_found
                "no auth available (providers=codex, model=gpt-5.4(xhigh))"
```

后果：beat_extraction 波几乎零填充（纯 GPT chat）；style 还在涨（走 NVIDIA embedding，不受冷却影响）。**凭证恢复后 beat 会自动跟上**，retry 机制本身以预期工作。

### 在冷却期间不要做的事

- 不要动 `model_router.py` 的 endpoint 配置
- 不要 force 重发 task（它自己在 30s loop）
- 不要担心“style 多 beat 少”的差异——这是凭证问题不是代码问题

## 5. 后续待办（用户叫到哪项才做，不要主动郄上去）

1. SAWarning 真源排查（上上个会话遗留）
2. N+1 query 深扫
3. 类型一致性审计
4. 全 codebase 搜剩余 `_get_db` pattern
5. **6 个保留的 May 13 分支决策**：`feat/pr-auth-ttl` 975403e / `feat/pr-b-critic-semantic-clarity` 5f05cab / `feat/pr-c-premise-structured` 091d238 / `feat/pr-dosage-audit` 5fe6cf7 / `feat/pr-profile-seed-rules` 64868a9 / `feat/pr-state-wiring-audit` 29a1e46
6. `retry_missing_branches` 自身 session 管理（L491，非 `async with`，需重构）
7. `reference_books.metadata_json` 列类型 `json` → `jsonb`（上个会话那个 `COALESCE could not convert type jsonb to json` 错误的根）
8. `_register_new_foreshadows` 偶发 180s timeout（设计内保护，低优）
9. LLM 输出长度 ≈3 米 vs. prompt 要求 200–400 字（prompt 不被遵守）

## 6. 环境参考

- **Repo**：`/root/ai-write`，分支 `main`，HEAD `6dda3c9`
- **Docker**：
  - `ai-write-backend-1` — uvicorn --reload，挂载 `/root/ai-write/backend -> /app rw`
  - `ai-write-celery-worker-1` — visibility_timeout=7200
  - `ai-write-postgres-1` — user/pwd `postgres`/`postgres`，db `aiwrite`，idle_in_transaction_session_timeout=10min
  - `ai-write-redis-1`
- **MCP tool 调用要点**：
  - `apply_patch` 参数名是 `patch`（不是 `input`）
  - `docker logs --since` 在这台机常超时——用 `--tail N` 替代
  - Shell 里 commit message 中文 × 小引号会被拆词 → 用 `git commit -F /tmp/commit_msg.txt`
- **Celery dispatch**：`from app.tasks import celery_app`（不是 `app.celery_app`）

## 7. 接手后的首次动作

1. 读本文件到点，不要重复读上个会话的走过的路
2. 用 §3 的 SQL 查一下《赤心巡天》当前进度 + decompile_retry metadata
3. 用 §3 的 docker logs --tail 看一眼 worker 是不是还在 30s loop 里
4. 告诉 King 当前状态，然后等他下一个指令。
5. 不要主动 offer 做 §5 里的任何一项。
